// Arquivo tema usado para importar as configurações das cores e das fontes para não ter que ficar fazendo
// múltiplas importações

import { colors } from "./colors"
import { fontFamily } from "./font-family"

export { colors, fontFamily }
